#!/usr/bin/python3
"""function to return a float"""


def floor(n: float) -> int:
    """returns the floor of a float.

    Args:
        n (float): the float number to find the floor of

    Returns:
        int: the floor of the float n, the largest integer <= n.
    """
    return floor(n)
